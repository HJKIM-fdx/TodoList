# STS4 문제 해결 빠른 체크리스트

## 🚨 exit code=1 에러 해결

### 1단계: 경로 확인
- [ ] `C:\Users\Public\FDX\STS\java\jdk1.8.0_181\jre\bin\javaw.exe` 존재 확인
- [ ] `C:\Users\Public\FDX\STS\lombok\lombok.jar` 존재 확인
- [ ] `C:\Users\Public\FDX\STS\sts-bundle\SpringToolSuite4.exe` 존재 확인

### 2단계: STS.ini 설정 확인
- [ ] `-vm`과 `-vmargs`가 별도 줄에 있는지 확인
- [ ] 경로에 공백이나 특수문자가 없는지 확인
- [ ] `STS_FINAL.ini`가 `SpringToolSuite4.ini`로 복사되었는지 확인

### 3단계: 권한 확인
- [ ] 관리자 권한으로 실행
- [ ] 폴더 쓰기 권한 확인
- [ ] 파일 실행 권한 확인

### 4단계: 워크스페이스 초기화
```batch
rmdir /s /q "C:\Users\Public\FDX\STS\workspace\.metadata"
```

## 🔧 Lombok 에러 해결

### 1단계: JAR 파일 확인
- [ ] lombok.jar 파일이 정확한 경로에 있는지 확인
- [ ] JAR 파일이 손상되지 않았는지 확인

### 2단계: 경로 설정 확인
- [ ] `-javaagent:C:\Users\Public\FDX\STS\lombok\lombok.jar` 경로 확인
- [ ] 경로에 공백이 없는지 확인

### 3단계: STS 재시작
- [ ] STS4 완전 종료
- [ ] STS4 재시작

## 📦 Gradle 빌드 에러 해결

### 1단계: 캐시 확인
- [ ] `C:\Users\Public\FDX\STS\gradle\cache` 폴더 존재 확인
- [ ] 필요한 의존성 JAR 파일들이 로컬에 있는지 확인

### 2단계: Offline Mode 설정
- [ ] STS4 → Window → Preferences → Gradle
- [ ] Offline Mode 체크
- [ ] Gradle User Home: `C:\Users\Public\FDX\STS\gradle\cache`

### 3단계: 프로젝트 설정 확인
- [ ] build.gradle 파일에서 로컬 저장소 설정 확인
- [ ] 의존성 경로가 로컬 경로로 설정되어 있는지 확인

## 🗂️ Maven 빌드 에러 해결

### 1단계: 로컬 저장소 확인
- [ ] `C:\Users\Public\FDX\STS\maven\repository` 폴더 존재 확인
- [ ] 필요한 의존성들이 로컬 저장소에 있는지 확인

### 2단계: Maven 설정 확인
- [ ] STS4 → Window → Preferences → Maven → User Settings
- [ ] Local Repository: `C:\Users\Public\FDX\STS\maven\repository`

### 3단계: pom.xml 확인
- [ ] 로컬 저장소 경로가 올바르게 설정되어 있는지 확인
- [ ] 의존성 버전이 로컬에 있는 버전과 일치하는지 확인

## 🔍 로그 확인 방법

### 1단계: STS 로그 확인
```batch
type "C:\Users\Public\FDX\STS\workspace\.metadata\.log"
```

### 2단계: Java 로그 확인
```batch
java -version
echo %JAVA_HOME%
```

### 3단계: 시스템 로그 확인
- [ ] 이벤트 뷰어에서 애플리케이션 오류 확인
- [ ] Windows 로그에서 Java 관련 오류 확인

## ⚡ 빠른 해결 방법

### 방법 1: 자동 설정 스크립트 사용
```batch
setup_sts.bat
```

### 방법 2: 수동 설정
1. 모든 폴더 구조 확인
2. STS_FINAL.ini 복사
3. 워크스페이스 초기화
4. STS4 재시작

### 방법 3: 완전 재설치
1. 기존 STS4 제거
2. 폴더 구조 재생성
3. 새로 설치
4. 설정 적용

## 📞 추가 도움

### 문제가 지속될 경우:
1. **로그 파일 확인**: `.metadata\.log` 파일 내용 확인
2. **Java 버전 확인**: `java -version` 명령어로 버전 확인
3. **경로 재확인**: 모든 경로가 정확한지 다시 한 번 확인
4. **권한 재확인**: 관리자 권한으로 실행

### 체크리스트 완료 후:
- [ ] 모든 항목이 체크되었는지 확인
- [ ] STS4가 정상적으로 시작되는지 확인
- [ ] 프로젝트가 정상적으로 빌드되는지 확인
- [ ] Lombok이 정상적으로 작동하는지 확인

이 체크리스트를 따라하면 대부분의 STS4 문제를 해결할 수 있습니다! 🚀